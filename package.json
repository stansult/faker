{
  "name": "faker",
  "private": true,
  "scripts": {
    "build:constants": "node scripts/build_constants.mjs",
    "build:itch": "node scripts/build_itch.mjs",
    "build:netlify": "npm run build:constants && node scripts/build_netlify.mjs",
    "check:syntax": "node --check app.js && node --check playwright.config.mjs && for f in netlify/functions/*.js scripts/*.cjs scripts/*.mjs tests/*.mjs tests/helpers/*.mjs tests/ui/*.mjs; do node --check \"$f\"; done",
    "test": "npm run test:logic && npm run test:deployment",
    "test:api": "node tests/run.mjs tests/*.smoke.test.mjs",
    "test:api:remote": "node tests/run.mjs tests/api.remote.test.mjs",
    "test:deployment": "node --test scripts/*.test.cjs",
    "test:logic": "node tests/run.mjs tests/blobConsistency.test.mjs tests/netlifyDev.test.mjs tests/roomCode.test.mjs tests/roomExpiry.test.mjs tests/vote.test.mjs",
    "test:ui": "playwright test",
    "dev": "VOTE_TOTAL_SECONDS=${VOTE_TOTAL_SECONDS:-300} VOTE_FINAL_SECONDS=${VOTE_FINAL_SECONDS:-10} npm run build:constants && netlify dev",
    "dev:lan": "VOTE_TOTAL_SECONDS=${VOTE_TOTAL_SECONDS:-300} VOTE_FINAL_SECONDS=${VOTE_FINAL_SECONDS:-10} npm run build:constants && IP=$(ipconfig getifaddr en0 || ipconfig getifaddr en1 || echo 'unknown'); if [ \"$IP\" = \"unknown\" ]; then echo \"Can't test on a different device: IP is unknown\"; else echo \"LAN test: http://${IP}:8888\"; fi; netlify dev --command \"python3 -m http.server 3999\" --target-port 3999 --functions netlify/functions --functions-port 9999 --port 8888",
    "dev:live": "bash scripts/dev_live.sh"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
